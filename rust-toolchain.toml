[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "default"

# The dev toolchain tracks stable so fmt and clippy match CI.
# The minimum supported Rust version is pinned separately as
# `rust-version` in Cargo.toml, and is checked by the `msrv` CI job.
