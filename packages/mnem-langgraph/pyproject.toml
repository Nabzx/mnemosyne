[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mnem-langgraph"
version = "0.0.1"
description = "LangGraph BaseStore backed by Mnemosyne: versioned agent memory with blame and branch."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "Nabil Shah", email = "nabilshahx@gmail.com" }]
keywords = ["ai", "agents", "memory", "langgraph", "version-control"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
]
dependencies = [
    "mnem-agents ~= 0.0",
    "langgraph >= 1.0, < 2",
]

[project.urls]
Homepage = "https://github.com/Nabzx/mnemosyne"
Repository = "https://github.com/Nabzx/mnemosyne"
Documentation = "https://nabzx.github.io/mnemosyne/"

[project.optional-dependencies]
dev = ["pytest >= 8", "ruff >= 0.6"]

# The SDK is not on PyPI yet; take it from the repo root.
[tool.uv.sources]
mnem-agents = { path = "../..", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["mnem_langgraph"]

[tool.ruff]
line-length = 100
